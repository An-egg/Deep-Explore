# DeepExplore Dependencies
# Core functionality has no external dependencies

# Optional YAML support (install with: pip install deep-explore[yaml])
pyyaml>=6.0

# Development dependencies (install with: pip install deep-explore[dev])
pytest>=7.0
pytest-cov>=4.0
black>=22.0
flake8>=5.0
mypy>=1.0
sphinx>=5.0
sphinx-rtd-theme>=1.0

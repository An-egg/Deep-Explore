# DeepExplore Production Dependencies
# No external dependencies are required for the core functionality

# Optional dependencies (install with: pip install deep-explore[yaml])
pyyaml>=6.0; extra == "yaml"

# Development dependencies (install with: pip install deep-explore[dev])
pytest>=7.0; extra == "dev"
pytest-cov>=4.0; extra == "dev"
black>=22.0; extra == "dev"
flake8>=5.0; extra == "dev"
mypy>=1.0; extra == "dev"
sphinx>=5.0; extra == "dev"
sphinx-rtd-theme>=1.0; extra == "dev"
